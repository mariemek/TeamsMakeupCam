import SwiftUI

/// Root view — simply hosts MainStudioView so TeamsMakeupCamApp
/// has a stable ContentView entry point.
struct ContentView: View {
    var body: some View {
        MainStudioView()
    }
}
